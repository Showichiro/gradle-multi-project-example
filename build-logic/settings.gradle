rootProject.name = 'build-logic'
include 'commons', 'java-library', 'spring-boot-application', 'openapi', 'spotless', 'lib'

dependencyResolutionManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}
includeBuild('../platforms')